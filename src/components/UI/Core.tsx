import styles from './Core.module.css';
import Card from './Card';

function Core(){
  return (
    <Card>
      <div className={styles.core}>Больше не успел(</div>
    </Card>
  )
};

export default Core;